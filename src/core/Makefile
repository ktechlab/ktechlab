# Makefile.in generated by unsermake
####################################

top_builddir = ../..
top_srcdir = ../..
srcdir = .
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	unsermake force-reedit

$(top_builddir)/src/core/Makefile: $(top_builddir)/config.status $(top_srcdir)/src/core/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status src/core/Makefile

all: $(top_builddir)/src/core/Makefile
final: 
	unsermake -C $(top_builddir)/src/core UNSERMAKE_FORCE_FINAL=1 libcore.la

METASOURCES = 
__um_includes_dcopclient.h = main.cpp
__um_includes_kaboutdata.h = main.cpp
__um_includes_kapplication.h = main.cpp
__um_includes_kcmdlineargs.h = main.cpp
__um_includes_kconfig.h = main.cpp
__um_includes_klocale.h = main.cpp
__um_includes_ktechlab.h = main.cpp
.PHONY: all final
